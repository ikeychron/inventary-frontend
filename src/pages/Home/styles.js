import { makeStyles } from "@material-ui/core";

export default makeStyles((Theme) => ({
  container: {
    width: "100%",
    height: "100vh",
    maxHeight: "100vh",
    display: "flex",
    flexDirection: "row-reverse",
  },
}));
